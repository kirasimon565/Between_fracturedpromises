import 'package:flutter/material.dart';

class AppColors {
  // Common
  static const Color glassOverlay = Color(0x99000000); // For that "Fractured" glass feel

  // Safe Theme (Messenger) - Now "Dark Noir" instead of White
  static const Color messengerPrimary = Color(0xFF2C2C2C); // Slate Charcoal
  static const Color messengerBackground = Color(0xFF0F0F0F); // Near Black
  static const Color messengerText = Color(0xFFE0E0E0);
  static const Color messengerBubbleSelf = Color(0xFF3A3A3C);
  static const Color messengerBubbleOther = Color(0xFF262626);

  // Secret Theme (Makelove) - "Obsession Red"
  static const Color makelovePrimary = Color(0xFF8B0000); // Blood Red
  static const Color makeloveBackground = Color(0xFF050505); // True Black
  static const Color makeloveText = Color(0xFFFFFFFF);
  static const Color makeloveBubbleSelf = Color(0xFF660000);
  static const Color makeloveBubbleOther = Color(0xFF1A1A1A);

  // Admin (Terminal)
  static const Color adminBackground = Color(0xFF000000);
  static const Color adminText = Color(0xFF00FF00); 
}
