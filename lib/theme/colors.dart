import 'package:flutter/material.dart';

class AppColors {
  // Safe Theme (Messenger)
  static const Color messengerPrimary = Color(0xFF0084FF);
  static const Color messengerBackground = Color(0xFFFFFFFF);
  static const Color messengerText = Color(0xFF000000);
  static const Color messengerBubbleSelf = Color(0xFF0084FF);
  static const Color messengerBubbleOther = Color(0xFFE4E6EB);

  // Secret Theme (Makelove)
  static const Color makelovePrimary = Color(0xFFFF2D55);
  static const Color makeloveBackground = Color(0xFF1C1C1E);
  static const Color makeloveText = Color(0xFFFFFFFF);
  static const Color makeloveBubbleSelf = Color(0xFFFF2D55);
  static const Color makeloveBubbleOther = Color(0xFF2C2C2E);

  // Admin
  static const Color adminBackground = Color(0xFF000000);
  static const Color adminText = Color(0xFF00FF00); // Terminal green
}
