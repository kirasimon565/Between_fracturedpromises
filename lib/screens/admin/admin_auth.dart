import 'package:flutter/material.dart';

class AdminAuth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Placeholder for robust auth check if needed beyond dialpad
    return Container();
  }
}
